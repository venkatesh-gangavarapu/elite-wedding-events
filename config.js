// Paste the Supabase project URL and anon/public key here.
// These are safe to expose in a browser when Row Level Security is configured correctly.
// NEVER put a Supabase service_role key in this file.
window.EWE_CONFIG = {
  url: "",
  anonKey: ""
};